"""Shared task-state schema and provenance logger (§6.6).

One JSON schema is shared by all workflows; memory is this state plus the tool-call
log. Free-text summaries may not replace structured results.
"""
